These are simple demo front-ends for Unicode/EWTS conversion, and Tibetan Phonetics.

The JS files in this folder are bundled by esbuild from the source files in `src/demo`.  Use `npm run build` to regenerate them.
